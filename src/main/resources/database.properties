# Database Connection Settings
db.url=jdbc:postgresql://localhost:5432/demodb
db.user=postgres
# IMPORTANT: Replace with your actual PostgreSQL password from installation
db.password=joshuang0827

# Connection Pool Settings
db.pool.initialSize=5
db.pool.maxSize=10
db.pool.timeout=30000

# Additional PostgreSQL Settings
db.autoReconnect=true
db.useSSL=false 